# Local dev SQLite databases (e.g. created by seed_demo.py / local backend runs)
*.db
gitsyntropy_local.db

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.venv/
venv/
ENV/
.mypy_cache/
.coverage
coverage.xml

# Node
node_modules/
**/node_modules/
dist/
**/dist/
.astro/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*
!.env.example

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
.claude/
.claude*
# Manuscript, reviewer correspondence, and revision working files — kept private.
# Untracked on purpose: the repository is public, and the paper pseudonymises the
# developers whose identities the raw research data would otherwise reveal.
paper/

# Agent/editor local config
.claude/
.claude*

# Research data — private identity mapping (Reviewer C #3 pseudonymisation)
scripts/data/pseudonym_map.csv
scripts/data/pseudonym_salt.txt

# Research script virtualenv
scripts/.venv/
